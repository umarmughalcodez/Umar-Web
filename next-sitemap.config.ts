/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: "https://umarweb.com", // replace with your domain
  generateRobotsTxt: true, // generates robots.txt automatically
  changefreq: "weekly",
  priority: 0.7,
  sitemapSize: 5000,
};
